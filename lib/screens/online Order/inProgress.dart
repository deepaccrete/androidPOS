import 'package:flutter/material.dart';

class Online_InProgress extends StatelessWidget {
  const Online_InProgress({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('InProgress'),);
  }
}
